#!/usr/bin/env ansible-playbook
---
- name: DHCP с failover и DDNS
  hosts: domain_controllers
  become: true
  become_method: su
  become_user: root
  gather_facts: true
  roles:
    - dhcp_server
...
