---
- name: Перезапуск squid 
  systemd:
    name: "{{ item }}"
    state: restarted
    enabled: true
    masked: false
    daemon_reload: true
  listen: "Перезапуск squid"
  async: 10
  poll: 0
  loop:
    - squid
  ignore_unreachable: true
...
